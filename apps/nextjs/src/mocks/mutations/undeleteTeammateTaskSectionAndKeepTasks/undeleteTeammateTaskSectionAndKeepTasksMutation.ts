// Generated by `yarn new:mutationMock`
import { graphql, HttpResponse } from 'msw';
import { data } from './data';
import type { Mutation, Options } from './type';

export const undeleteTeammateTaskSectionAndKeepTasksMutation = (
  options?: Options,
) => {
  return graphql.mutation<Mutation>(
    'UndeleteTeammateTaskSectionAndKeepTasks',
    () => {
      if (options?.networkError) {
        return HttpResponse.json({
          errors: [
            {
              message: 'Network request failed',
            },
          ],
        });
      }

      return HttpResponse.json({
        data: data(options?.res, options?.deepMergeOptions),
      });
    },
  );
};
